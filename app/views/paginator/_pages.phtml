<nav aria-label="Page navigation" class="mt-4">
    <ul class="inline-flex items-center space-x-1 justify-end">

        <?php if ($paginator->hasPreviousPage()) : ?>
            <li>
                <a href="<?= route($paginator->getRouteName(), ['page' => $paginator->previousPage()]) ?>"
                    class="px-3 py-1 rounded-md border border-gray-300 bg-white text-gray-700 hover:bg-gray-100">
                    Anterior
                </a>
            </li>
        <?php else : ?>
            <li>
                <span class="px-3 py-1 rounded-md border border-gray-300 bg-gray-200 text-gray-500 cursor-not-allowed">
                    Anterior
                </span>
            </li>
        <?php endif; ?>

        <?php for ($page = 1; $page <= $paginator->totalOfPages(); $page++) : ?>
            <li>
                <a href="<?= route($paginator->getRouteName(), ['page' => $page]) ?>"
                    class="<?= $paginator->isPage($page)
                                ? 'px-3 py-1 rounded-md border border-blue-500 bg-blue-500 text-white font-semibold'
                                : 'px-3 py-1 rounded-md border border-gray-300 bg-white text-gray-700 hover:bg-gray-100' ?>">
                    <?= $page ?>
                </a>
            </li>
        <?php endfor; ?>

        <?php if ($paginator->hasNextPage()) : ?>
            <li>
                <a href="<?= route($paginator->getRouteName(), ['page' => $paginator->nextPage()]) ?>"
                    class="px-3 py-1 rounded-md border border-gray-300 bg-white text-gray-700 hover:bg-gray-100">
                    Próxima
                </a>
            </li>
        <?php else : ?>
            <li>
                <span class="px-3 py-1 rounded-md border border-gray-300 bg-gray-200 text-gray-500 cursor-not-allowed">
                    Próxima
                </span>
            </li>
        <?php endif; ?>

    </ul>
</nav>

<div class="flex justify-end mt-2 text-sm text-gray-600">
    <?= $paginator->entriesInfo() ?>
</div>